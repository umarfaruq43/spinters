import Navbar from "@/Components/Navbar";
import React from "react";

const faqs = () => {
    return (
        <div>
            <Navbar activeNav={"faqs"} />
        </div>
    );
};

export default faqs;
