import Faqs from "@/Components/Faq/Faqs";

import Layout from "@/Layout/Layout";
import React from "react";

const faqs = () => {
    return (
        <Layout>
            <Faqs />
        </Layout>
    );
};

export default faqs;
